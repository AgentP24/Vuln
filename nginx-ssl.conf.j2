# VulnGuard AI - Nginx SSL Hardening Configuration
# Generated: {{ ansible_date_time.iso8601 }}
# Vulnerability ID: {{ vulnerability_id }}

# Disable deprecated SSL/TLS versions
ssl_protocols TLSv1.2 TLSv1.3;

# Strong cipher suites only (in order of preference)
ssl_ciphers '{{ ssl_ciphers }}';

# Server chooses cipher suite
ssl_prefer_server_ciphers on;

# Enable session resumption for performance
ssl_session_timeout 1d;
ssl_session_cache shared:SSL:50m;
ssl_session_tickets off;

# OCSP Stapling
ssl_stapling on;
ssl_stapling_verify on;

# DNS resolver for OCSP
resolver 8.8.8.8 8.8.4.4 valid=300s;
resolver_timeout 5s;

# Diffie-Hellman parameters
# Note: Generate with: openssl dhparam -out /etc/nginx/dhparam.pem 4096
# ssl_dhparam /etc/nginx/dhparam.pem;

# HSTS (optional but recommended)
# Uncomment after verifying HTTPS works correctly
# add_header Strict-Transport-Security "max-age=63072000" always;
